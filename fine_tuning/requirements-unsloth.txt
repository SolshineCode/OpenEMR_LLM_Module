# Unsloth Fine-Tuning Requirements
# Install in a separate environment for GPU training
#
# For NVIDIA GPUs:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install --no-deps "trl<0.9.0" peft accelerate bitsandbytes
#
# For Google Colab (recommended for free GPU):
#   Just run the notebook in fine_tuning/train_medical_llm.ipynb

# Core training dependencies
torch>=2.1.0
transformers>=4.37.0
datasets>=2.16.0
accelerate>=0.26.0
peft>=0.8.0
trl>=0.7.0,<0.9.0
bitsandbytes>=0.42.0

# Unsloth (install from git for latest)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# Evaluation
evaluate>=0.4.0
rouge-score>=0.1.2
nltk>=3.8.0

# Model export
gguf>=0.1.0  # For exporting to GGUF format for llama.cpp

# Optional: Weights & Biases for experiment tracking
# wandb>=0.16.0

# Optional: MLflow for model registry
# mlflow>=2.10.0
